{
  "model": "{{ if eq (index . "machineType" | default "personal") "work" }}opus{{ else }}sonnet{{ end }}",
  "output-style": "Learning",
  "hooks": {
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "terminal-notifier -title 'Claude Code' -message 'Awaiting input' -sound default"
          }
        ]
      }
    ]
  }
}
