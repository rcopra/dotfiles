# Documentation and planning (repo-only, not applied to home)
README.md
LICENSE
CLAUDE.md
docs
docs/**

# ==============================================================================
# VS Code settings (managed via symlinks, not direct copies)
# ==============================================================================
# These source files live in the repo root and are symlinked to Library/
# This prevents chezmoi from trying to copy them to the home directory
vscode_settings.json
vscode_keybindings.json

# ==============================================================================
# macOS-only files - exclude on Linux
# ==============================================================================
{{ if ne .chezmoi.os "darwin" }}
# AeroSpace window manager (macOS only)
.aerospace.toml

# Karabiner keyboard remapper (macOS only)
.config/karabiner
.config/karabiner/**

# VS Code settings (macOS Library path)
Library
Library/**
{{ end }}

# ==============================================================================
# Server/headless machines - exclude GUI apps
# ==============================================================================
{{ if eq (index . "machineType" | default "personal") "server" }}
# Window managers
.aerospace.toml
.config/karabiner
.config/karabiner/**

# Terminal emulator config (use default terminal on servers)
.wezterm.lua

# VS Code (not installed on servers)
Library
Library/**
{{ end }}

# ==============================================================================
# Claude Code runtime/state (keep local to each machine)
# ==============================================================================
.claude/cache
.claude/cache/**
.claude/debug
.claude/debug/**
.claude/downloads
.claude/downloads/**
.claude/file-history
.claude/file-history/**
.claude/history.jsonl
.claude/paste-cache
.claude/paste-cache/**
.claude/plans
.claude/plans/**
.claude/plugins
.claude/plugins/**
.claude/projects
.claude/projects/**
.claude/session-env
.claude/session-env/**
.claude/shell-snapshots
.claude/shell-snapshots/**
.claude/stats-cache.json
.claude/statsig
.claude/statsig/**
.claude/telemetry
.claude/telemetry/**
.claude/todos
.claude/todos/**
.claude/settings.local.json
