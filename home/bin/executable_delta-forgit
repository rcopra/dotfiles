#!/bin/sh
# Delta wrapper for forgit's fzf preview. Cannot use delta's
# --detect-dark-light here: the OSC query reply races into fzf's
# stdin and shows up as typed input. Theme is always Catppuccin
# Macchiato now, so pin dark.
exec delta --no-gitconfig --dark "$@"
