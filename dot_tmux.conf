set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"

set -g history-limit 50000

# Start windows and panes at 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

set -g window-style 'bg=default'
set -g window-active-style 'bg=default'

set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

unbind %
bind | split-window -h

unbind '"'
bind - split-window -v

unbind r
bind r source-file ~/.tmux.conf

bind -r Down resize-pane -D 5
bind -r Up resize-pane -U 5
bind -r Right resize-pane -R 5
bind -r Left resize-pane -L 5

bind -r m resize-pane -Z

bind M-c attach-session -c "#{pane_current_path}"

# Tmux sessionizer - ThePrimeagen style
bind-key -r f run-shell "tmux neww ~/bin/tmux-sessionizer"

# Quick session switchers
bind-key -r C-s run-shell "tmux switch-client -t SSH || tmux new-session -d -s SSH && tmux switch-client -t SSH"

# Quick directory shortcuts
bind-key -r C-d run-shell "~/bin/tmux-sessionizer ~/.local/share/chezmoi"
bind-key -r C-n run-shell "~/bin/tmux-sessionizer ~/.config/nvim"
bind-key -r C-w run-shell "~/bin/tmux-sessionizer ~/work"
bind-key -r C-p run-shell "~/bin/tmux-sessionizer ~/personal"

set -g mouse on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection

# ==============================================================================
# Clipboard Integration (OSC 52 - works over SSH)
# ==============================================================================
# Use tmux's built-in OSC 52 support (set-clipboard on below handles this)
# This works on macOS, Linux, and over SSH with modern terminals
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel
bind-key -T copy-mode-vi MouseDragEnd1Pane send -X copy-selection-and-cancel
bind-key -T copy-mode-vi Enter send -X copy-selection-and-cancel


# remove delay for exiting insert mode with ESC in Neovim
set -sg escape-time 10

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

# list of tmux plugins
set -g @plugin 'christoomey/vim-tmux-navigator'

# vim-tmux-navigator: use Ctrl+Arrow for pane navigation
set -g @vim_navigator_mapping_left "C-Left"
set -g @vim_navigator_mapping_right "C-Right"
set -g @vim_navigator_mapping_up "C-Up"
set -g @vim_navigator_mapping_down "C-Down"

set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'

# Fix mouse click behavior
bind-key -T root MouseDown1Pane select-pane -t = \; send-keys -M

# Enable focus events so applications can detect when you switch panes
set -g focus-events on

# Enable clipboard passthrough and OSC 52 support
set -g allow-passthrough on
set -g set-clipboard on

# ==============================================================================
# Rose Pine Moon theme (manual)
# ==============================================================================
# Palette reference:
#   base=#232136  surface=#2a273f  overlay=#393552
#   muted=#6e6a86  subtle=#908caa  text=#e0def4
#   love=#eb6f92  gold=#f6c177  rose=#ea9a97
#   pine=#3e8fb0  foam=#9ccfd8  iris=#c4a7e7
#   hl_med=#44415a

# Status bar base
set -g status-style "bg=#232136,fg=#e0def4"
set -g status-justify centre
set -g status-left-length 100
set -g status-right-length 100

# Left: session name in pill (love on prefix, iris otherwise)
set -g status-left "#[fg=#393552,bg=default]#(printf '\uE0B6')#[bg=#393552,fg=#{?client_prefix,#eb6f92,#c4a7e7}] #S #[fg=#393552,bg=default]#(printf '\uE0B0')"

# Right: hostname in pill (foam)
set -g status-right "#[fg=#393552,bg=default]#(printf '\uE0B2')#[bg=#393552,fg=#9ccfd8] #h #[fg=#393552,bg=default]#(printf '\uE0B4')"

# Window list
set -g window-status-separator ""
set -g window-status-format "  #[fg=#ea9a97]#I#[fg=#6e6a86]:#[fg=#ea9a97]#W  "
set -g window-status-current-format "#[fg=#393552,bg=default]#(printf '\uE0B6')#[bg=#393552,fg=#f6c177]#I#[fg=#c4a7e7]:#[fg=#f6c177]#W#[fg=#393552,bg=default]#(printf '\uE0B4')"

# Pane borders
set -g pane-border-style "fg=#44415a"
set -g pane-active-border-style "fg=#f6c177"

# Messages
set -g message-style "fg=#6e6a86,bg=#232136"
set -g message-command-style "fg=#232136,bg=#f6c177"

# Clock & copy mode
setw -g clock-mode-colour "#eb6f92"
setw -g mode-style "bg=#44415a"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
